// No-op stub for `server-only` used by vitest. The real package intentionally
// throws if imported outside a React Server Component. In node tests we let
// server modules be imported normally.
export {};
