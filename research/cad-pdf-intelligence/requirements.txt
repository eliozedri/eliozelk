# CAD PDF Intelligence POC — isolated research dependencies
# Install: .venv/bin/pip install -r requirements.txt

# Core — required for all stages
PyMuPDF>=1.26.0       # PDF parsing, vector extraction, page rendering
pdfplumber>=0.11.0    # char-level text extraction, supplemental analysis
Pillow>=10.0.0        # image handling, PNG I/O

# Stage 4 — symbol clustering
numpy>=1.24.0         # array operations, centroid matrix
scipy>=1.10.0         # DBSCAN clustering

# Stage 5 — debug overlay
# SVG written directly (no extra dep). Pillow handles PNG.

# Stage E — sign template matching (installed)
opencv-python-headless>=4.8.0

# Stage G/H — Vision API sign-code reading (installed 2026-05-19)
anthropic>=0.103.0            # Claude Vision API client

# Stage G — Local OCR diagnostic (installed 2026-05-19)
# System dep: brew install tesseract  (tesseract 5.5.2, eng+snum data)
pytesseract>=0.3.13           # thin pytesseract wrapper around tesseract binary

# Stage S20 — Worker/Operations Export (installed 2026-05-21)
openpyxl>=3.1.0                # Excel workbook generation

# Optional — future stages
# svgwrite>=1.4.3                # alternative SVG generation
# weasyprint>=60.0               # PDF generation from HTML (optional follow-up)

# Image Scanner Engine (installed 2026-05-21)
easyocr>=1.7.2                 # multi-language OCR; numeric sign codes + Hebrew annotations
paddleocr>=3.5.0               # alternative OCR engine for comparison
paddlepaddle>=3.3.1            # PaddlePaddle backend for PaddleOCR
scikit-image>=0.24.0           # connected components, morphological ops, local peak detection
shapely>=2.0.7                 # geometric containment/proximity queries for spatial association
ezdxf>=1.4.2                   # DXF reading for plans delivered as DXF (future use)
rtree>=1.4.1                   # spatial indexing for fast proximity search (needs: brew install spatialindex)
# System dep: brew install tesseract-lang  (adds heb.traineddata + 100+ languages to tesseract)
